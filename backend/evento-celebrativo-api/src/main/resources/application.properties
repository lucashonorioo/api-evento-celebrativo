spring.application.name=API de Eventos Celebrativos

spring.profiles.active=${APP_PROFILE:local}
spring.jpa.open-in-view=false
app.time-zone=${APP_TIME_ZONE:America/Sao_Paulo}

security.client-id=${CLIENT_ID:myclientid}
security.client-secret=${CLIENT_SECRET:myclientsecret}
security.jwt.duration=${JWT_DURATION:86400}
security.oauth-token-url=${OAUTH_TOKEN_URL:http://localhost:8080/oauth2/token}
cors.origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5173,http://localhost:4200}

app.notifications.schedule-conflict-resolution.enabled=${SCHEDULE_CONFLICT_RESOLUTION_ENABLED:true}
app.notifications.schedule-conflict-resolution.fixed-delay=${SCHEDULE_CONFLICT_RESOLUTION_FIXED_DELAY:PT15M}
app.notifications.schedule-conflict-resolution.batch-size=${SCHEDULE_CONFLICT_RESOLUTION_BATCH_SIZE:100}
